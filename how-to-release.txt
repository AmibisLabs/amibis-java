
Maven only and Pure Java only:
* scope: jOMiSCID-Maven
* clean
* build
* check tests are passing
* set proper version (e.g. 1.6.0) in pom.xml
* mvn -Prelease -Dmaven.test.skip=true deploy
* /bin/cp -r output-repository/* /home/oberon/public-maven/
* chmod -R g+rw /home/oberon/public-maven/*

Maven only and netbeans module only:
* scope: jOMiSCID-Netbeans
* build
* deploy
* scp -r output-repository/* oberon:/var/www/release/maven
